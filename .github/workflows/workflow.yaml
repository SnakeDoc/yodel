name: Workflow

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.number || github.ref }}
  cancel-in-progress: true

jobs:
  prettier-format-check:
    name: Format Check
    uses: SnakeDoc/yodel/.github/workflows/prettier-format-check.yaml@bf7494e28704e99d002d5a15b3ee2b744042694e # main

  markdown-lint-check:
    name: Lint Check
    uses: SnakeDoc/yodel/.github/workflows/markdown-lint-check.yaml@bf7494e28704e99d002d5a15b3ee2b744042694e # main

  gleam-format-check:
    name: Format Check
    uses: SnakeDoc/yodel/.github/workflows/gleam-format-check.yaml@bf7494e28704e99d002d5a15b3ee2b744042694e # main

  gleam-tests:
    name: Tests
    needs: [prettier-format-check, markdown-lint-check, gleam-format-check]
    uses: SnakeDoc/yodel/.github/workflows/gleam-tests.yaml@bf7494e28704e99d002d5a15b3ee2b744042694e # main
