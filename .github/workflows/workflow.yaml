name: Workflow

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.number || github.ref }}
  cancel-in-progress: true

jobs:
  prettier-format-check:
    name: Format Check
    uses: ./.github/workflows/prettier-format-check.yaml

  markdown-lint-check:
    name: Lint Check
    uses: ./.github/workflows/markdown-lint-check.yaml

  gleam-format-check:
    name: Format Check
    uses: ./.github/workflows/gleam-format-check.yaml

  gleam-tests:
    name: Tests
    needs: [prettier-format-check, markdown-lint-check, gleam-format-check]
    uses: ./.github/workflows/gleam-tests.yaml
